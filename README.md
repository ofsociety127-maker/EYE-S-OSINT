# EYE-S-OSINT
Its a osint tool where u can use it for fun or other stuff :=:

So u gotta pip install:
pip install requests
pip install dnspython
pip install python-whois
pip install pycryptodome
pip install pypiwin32


USE FOR EDUCATIONAL PUPRPOSE THIS CAN DO HARM!
